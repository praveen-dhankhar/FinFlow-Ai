# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=financeapp
DB_USERNAME=postgres
DB_PASSWORD=your_password_here
DB_SSLMODE=require

# JWT Configuration (CRITICAL: Generate a strong random secret for production)
JWT_SECRET=your-super-secret-key-here-minimum-256-characters-random-string
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# CORS Configuration (Update with your actual frontend domain)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app

# Spring Profile
SPRING_PROFILES_ACTIVE=prod
